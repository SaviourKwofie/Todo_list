<!-- <?php
// Database connection parameters
// $host = 'localhost'; 
// $username = 'root';
// $dbname = 'todo'; 
// $password = ''; 

// ini_set('display_errors', 1);
// ini_set('display_startup_errors', 1);
// error_reporting(E_ALL);

// // Enable CORS (optional, for cross-origin requests)
// header("Access-Control-Allow-Origin: *"); 
// header("Access-Control-Allow-Methods: GET, POST, OPTIONS");
// header("Access-Control-Allow-Headers: Content-Type"); 
// // Content-type for JSON response
// header("Allow-Access-Control-Origin: http://localhost:5500");
// header('Content-Type: application/json');
//  try { 
//     // Create a new PDO instance
//     $pdo = new PDO("mysql:host=$host;dbname=$dbname", $username, $password); $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
//      // Write your query
//      $stmt = $pdo->prepare("SELECT * FROM todos"); 
//      $stmt->execute(); 
//      // Fetch the data as an associative array
//      $data = $stmt->fetchAll(PDO::FETCH_ASSOC); // Return the data as a JSON responseecho 
//      echo json_encode($data); 
//     } catch (PDOException $e) { 
//         // Handle connection errors
//         echo json_encode([ 'error' => 'Database error: ' . $e->getMessage() ]);
//      }
  
    



 
